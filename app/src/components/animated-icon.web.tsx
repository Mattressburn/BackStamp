/**
 * Web has no native splash screen to hand off from, so there is nothing to cover.
 * The native file's overlay exists purely to hide that handoff.
 */
export function AnimatedSplashOverlay() {
  return null;
}
