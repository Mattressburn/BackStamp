node_modules/

# Secrets. .env.example is committed; .env never is.
.env
.env.local
*.env

# Runtime state: SQLite, uploaded photos, Expo caches, build output.
backend/data/
app/.expo/
dist/
*.sqlite
*.sqlite-wal
*.sqlite-shm

# Book digitizer working files — page scans and raw extractions are not source.
scripts/pages/
scripts/extracted*.json

.DS_Store
*.log

# Browser-automation scratch from design QA runs.
.playwright-mcp/

# CMoG page cache: raw fetched text is their copyrighted prose, never committed.
# Restated facts derived from it live in data/catalog.json, which is.
data/cmog-cache/
